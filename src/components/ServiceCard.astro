---
import type { Service } from '../data/services';
export interface Props {
  service: Service;
  index?: number;
}
const { service, index = 0 } = Astro.props;
const lazy = index > 2;
---

<article id={`service-${service.slug}`} class="card-glass group overflow-hidden scroll-mt-header">
  <div class="aspect-[4/3] overflow-hidden bg-white/5">
    <img
      src={service.image}
      alt={service.title}
      loading={lazy ? 'lazy' : 'eager'}
      decoding="async"
      class="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105"
      width="600"
      height="450"
    />
  </div>
  <div class="p-6">
    <h3 class="text-lg font-semibold text-accent-blue">{service.title}</h3>
    <p class="mt-2 text-sm text-matte-blue leading-relaxed">{service.short}</p>
    <a href="/kontakt" class="mt-4 inline-flex items-center gap-1 text-xs font-semibold text-emerald hover:text-emerald-light transition">
      Angebot anfragen
      <svg class="h-3 w-3 transition group-hover:translate-x-0.5" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd" /></svg>
    </a>
  </div>
</article>
